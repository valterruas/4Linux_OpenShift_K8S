version: '3'

services:
 site:
  image: valterruas/ivar-site
  hostname: ivar
  container_name: ivarsite
  ports:
   - 8081:80

version: '3'

volumes:
 repo:


services:
 site:
  image: gpolicante/ivar-site
  hostname: ivar
  container_name: ivarsite
  ports:
   - 8085:80
  volumes:
   - repo:/usr/local/apache2/htdocs
   - /srv/backup:/data

 monitoracao:
  image: google/cadvisor
  ports:
   - 8585:8080
  volumes:
    - /:/rootfs:ro
    - /var/run:/var/run:ro
    - /sys:/sys:ro
    - /var/lib/docker:/var/lib/docker:ro
    - /dev/disk:/dev/disk:ro
